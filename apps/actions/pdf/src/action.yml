name: 'action'
description: 'Build Resume to PDF'

inputs:
  id:
    description: 'ID of the resume to build'
    required: true

outputs:
  data:
    description: 'temp output'

runs:
  using: node20
  main: dist/index.js